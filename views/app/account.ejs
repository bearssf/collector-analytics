<!DOCTYPE html>
<html lang="en">
<head>
  <!-- billing: <%= typeof stripeElementsConfigured !== 'undefined' && stripeElementsConfigured ? 'subscribe-on-site' : 'checkout-redirect' %> -->
  <!-- deploy: git <%= process.env.RENDER_GIT_COMMIT || 'unknown' %> -->
  <%- include('../partials/app-head', { title: 'Account' }) %>
</head>
<body class="app-body app-body--account-page" data-subscription-success="<%= typeof subscriptionSuccessReturn !== 'undefined' && subscriptionSuccessReturn ? '1' : '' %>">
  <div class="app-grid app-grid--account">
    <%- include('../partials/app-sidebar', { user: user, navActive: 'account', projects: projects, currentProjectId: currentProjectId, workspaceSlug: '', appAccess: appAccess }) %>
    <main class="app-canvas">
      <h1>Account</h1>
      <p class="sub">Here you can manage your profile, subscription, and associated billing details.</p>

      <% if (typeof billingFlash !== 'undefined' && billingFlash) { %>
        <div class="account-flash account-flash--<%= billingFlash.kind %>"><%= billingFlash.text %></div>
      <% } %>
      <% if (typeof subscriptionSuccessReturn !== 'undefined' && subscriptionSuccessReturn && !appAccess.paid) { %>
        <div id="account-billing-pending" class="account-billing-pending">
          <p class="account-billing-pending__text">Stripe is confirming your membership — this usually takes a few seconds. We’ll reload when you’re active, or <button type="button" id="account-billing-refresh-btn" class="account-billing-pending__btn">refresh status</button>.</p>
          <p id="account-billing-poll-status" class="account-meta account-billing-pending__meta" hidden>Still confirming…</p>
        </div>
      <% } %>

      <% var showSubMaintain = stripeConfigured && subscriptionRow && subscriptionRow.stripe_subscription_id && (appAccess.paid || subscriptionRow.status === 'past_due'); %>
      <% var capEnd = subscriptionRow && (subscriptionRow.cancel_at_period_end === true || subscriptionRow.cancel_at_period_end === 1); %>
      <% var showYearlyMonthlyWindowNotice = showSubMaintain && typeof billingPriceMode !== 'undefined' && billingPriceMode === 'dual' && currentPlanInterval === 'year' && typeof within30DaysOfRenewal !== 'undefined' && !within30DaysOfRenewal; %>

      <div class="app-placeholder account-block">
        <div class="account-layout">
          <div class="account-layout__profile">
            <h2 class="account-h2">Profile</h2>
            <p class="account-meta account-meta--profile">Update your details below. Email is your sign-in identity and can’t be changed.</p>
            <form id="account-profile-form" class="app-form-stack account-form" autocomplete="off">
              <% var _titles = typeof allowedTitles !== 'undefined' && allowedTitles.length ? allowedTitles : ['Mr.', 'Mrs.', 'Ms.', 'Miss', 'Mx.', 'Dr.']; %>
              <label class="app-field">
                <span>Title: <abbr class="account-required-star" title="required">*</abbr></span>
                <select name="title" required>
                  <option value="" disabled <%= !profile.title ? 'selected' : '' %>>Select…</option>
                  <% _titles.forEach(function (t) { %>
                    <option value="<%= t %>" <%= profile.title === t ? 'selected' : '' %>><%= t %></option>
                  <% }); %>
                </select>
              </label>
              <label class="app-field">
                <span>First Name: <abbr class="account-required-star" title="required">*</abbr></span>
                <input type="text" name="firstName" required maxlength="100" value="<%= profile.firstName || '' %>" />
              </label>
              <label class="app-field">
                <span>Last Name: <abbr class="account-required-star" title="required">*</abbr></span>
                <input type="text" name="lastName" required maxlength="100" value="<%= profile.lastName || '' %>" />
              </label>
              <label class="app-field">
                <span>Email: <abbr class="account-required-star" title="required">*</abbr></span>
                <input type="email" value="<%= profile.email || user.email %>" readonly aria-readonly="true" />
              </label>
              <label class="app-field">
                <span>University:</span>
                <input type="text" name="university" list="account-university-list" maxlength="255" placeholder="Type or choose from list" value="<%= profile.university || '' %>" />
                <datalist id="account-university-list">
                  <% (typeof universities !== 'undefined' ? universities : []).forEach(function (u) { %>
                    <option value="<%= u %>"></option>
                  <% }); %>
                </datalist>
              </label>
              <label class="app-field">
                <span>Research Focus:</span>
                <textarea name="researchFocus" rows="3" maxlength="8000" placeholder="i.e. Cognition, Leadership, AI Impacts, Gender Representation in STEM, etc."><%= profile.researchFocus || '' %></textarea>
              </label>
              <label class="app-field">
                <span>Preferred Search Engine:</span>
                <select name="preferredSearchEngine">
                  <option value="" <%= !profile.preferredSearchEngine ? 'selected' : '' %>>Select…</option>
                  <% (typeof searchEngines !== 'undefined' ? searchEngines : []).forEach(function (s) { %>
                    <option value="<%= s %>" <%= profile.preferredSearchEngine === s ? 'selected' : '' %>><%= s %></option>
                  <% }); %>
                </select>
              </label>
              <div id="account-profile-msg" class="account-form-msg" role="status" aria-live="polite" hidden></div>
              <div class="app-form-actions app-form-actions--account-save">
                <button type="submit" class="app-btn-primary" id="account-profile-submit">Save profile</button>
              </div>
              <p class="account-form-required-note"><abbr class="account-required-star" title="required">*</abbr> Required Fields</p>
            </form>
          </div>

          <div class="account-layout__aside">
            <section class="account-rail-section">
              <h2 class="account-h2">Password</h2>
              <form id="account-password-form" class="app-form-stack account-form" autocomplete="off">
                <label class="app-field account-password-field">
                  <span>Current Password: <abbr class="account-required-star" title="required">*</abbr></span>
                  <span class="account-password-field__inner">
                    <input type="password" name="currentPassword" required autocomplete="current-password" />
                    <button type="button" class="account-password-toggle account-password-toggle--eye" data-password-toggle data-password-label="current password" aria-label="Show current password" aria-pressed="false" title="Show password">
                      <%- include('../partials/password-visibility-icons') %>
                    </button>
                  </span>
                </label>
                <label class="app-field account-password-field">
                  <span>New Password: <abbr class="account-required-star" title="required">*</abbr></span>
                  <span class="account-password-field__inner">
                    <input type="password" name="newPassword" required minlength="8" autocomplete="new-password" />
                    <button type="button" class="account-password-toggle account-password-toggle--eye" data-password-toggle data-password-label="new password" aria-label="Show new password" aria-pressed="false" title="Show password">
                      <%- include('../partials/password-visibility-icons') %>
                    </button>
                  </span>
                </label>
                <label class="app-field account-password-field">
                  <span>Confirm New Password: <abbr class="account-required-star" title="required">*</abbr></span>
                  <span class="account-password-field__inner">
                    <input type="password" name="confirmPassword" required minlength="8" autocomplete="new-password" />
                    <button type="button" class="account-password-toggle account-password-toggle--eye" data-password-toggle data-password-label="confirm new password" aria-label="Show confirm new password" aria-pressed="false" title="Show password">
                      <%- include('../partials/password-visibility-icons') %>
                    </button>
                  </span>
                </label>
                <div id="account-password-msg" class="account-form-msg" role="status" aria-live="polite" hidden></div>
                <div class="app-form-actions app-form-actions--account-save">
                  <button type="submit" class="app-btn-primary" id="account-password-submit">Update password</button>
                </div>
              </form>
            </section>

            <hr class="account-section-rule" aria-hidden="true" />

            <section class="account-rail-section">
              <h2 class="account-h2">Subscription</h2>
              <% if (typeof billingSummary !== 'undefined' && billingSummary.lines && billingSummary.lines.length) { %>
                <dl class="account-billing-summary" aria-label="Subscription details">
                  <% billingSummary.lines.forEach(function (row) { %>
                    <div class="account-billing-summary-row">
                      <dt><%= row.label %></dt>
                      <dd><%= row.value %></dd>
                    </div>
                  <% }); %>
                </dl>
              <% } %>
              <% if (appAccess.paid) { %>
                <p class="account-status account-status--paid">You have an <strong>active</strong> membership. The Foundry and member features are unlocked.</p>
              <% } else if (subscriptionRow && subscriptionRow.status === 'past_due') { %>
                <p class="account-status">Your last payment failed (<strong>past due</strong>). Use <strong>Update Card on File</strong> below or the Stripe emails you receive to update your payment method.</p>
              <% } else if (appAccess.trialing && appAccess.trialEndsLabel) { %>
                <p class="account-status">Free trial active. Upgrade to continue forging and to unlock all features.</p>
              <% } else { %>
                <p class="account-status">No active paid subscription.</p>
              <% } %>

              <% if (showYearlyMonthlyWindowNotice) { %>
                <p class="account-meta account-meta--subscription-notice">Switching to monthly billing is available within 30 days of your renewal<% if (typeof renewalDateLabel !== 'undefined' && renewalDateLabel) { %> (next renewal: <%= renewalDateLabel %>)<% } %>.</p>
              <% } %>

              <% if (showSubMaintain) { %>
                <div class="account-billing-maintain" id="account-subscription-actions">
                  <% if (capEnd) { %>
                    <button type="button" class="app-btn-primary" data-action="resume-subscription">Resume auto-renewal</button>
                    <p class="account-meta">Restores renewal after the date above. You can turn auto-renew off again anytime.</p>
                  <% } else { %>
                    <button type="button" class="app-btn-primary account-btn-yearly" data-action="cancel-at-period-end">Turn off auto-renewal</button>
                  <% } %>
                  <div id="account-subscription-msg" class="account-form-msg" hidden></div>
                </div>
              <% } %>

              <% if (showSubMaintain && typeof billingPriceMode !== 'undefined' && billingPriceMode === 'dual' && (currentPlanInterval === 'month' || (currentPlanInterval === 'year' && typeof within30DaysOfRenewal !== 'undefined' && within30DaysOfRenewal))) { %>
                <div class="account-plan-switch" id="account-plan-switch">
                  <% if (currentPlanInterval === 'month') { %>
                    <button type="button" class="app-btn-primary account-btn-yearly" data-action="switch-plan" data-interval="year">Switch to yearly billing</button>
                    <p id="account-plan-preview-estimate" class="account-plan-preview-estimate" hidden aria-live="polite"></p>
                  <% } else { %>
                    <button type="button" class="app-btn-primary" data-action="switch-plan" data-interval="month">Switch to monthly billing</button>
                    <p class="account-meta">Stripe will prorate the rest of this billing period (charge or credit on your next invoice).</p>
                  <% } %>
                  <div id="account-plan-switch-msg" class="account-form-msg" hidden></div>
                </div>
              <% } %>

              <% if (stripeConfigured && subscriptionRow && subscriptionRow.stripe_customer_id) { %>
                <p class="account-payment-method-link">
                  <a href="/billing/payment-method" class="app-link-cta">Update payment method</a>
                </p>
              <% } %>

              <% if (!appAccess.paid) { %>
                <% if (stripeConfigured) { %>
                  <% if (billingPriceMode === 'dual') { %>
                    <div class="account-billing-choices">
                      <% if (typeof stripeElementsConfigured !== 'undefined' && stripeElementsConfigured) { %>
                        <a class="app-btn-primary" href="/billing/subscribe?interval=month">Subscribe to Monthly Plan</a>
                        <a class="app-btn-primary account-btn-yearly" href="/billing/subscribe?interval=year">Subscribe to Annual Plan</a>
                      <% } else { %>
                        <a class="app-btn-primary" href="/billing/checkout?interval=month">Subscribe to Monthly Plan</a>
                        <a class="app-btn-primary account-btn-yearly" href="/billing/checkout?interval=year">Subscribe to Annual Plan</a>
                      <% } %>
                    </div>
                    <% if (typeof stripeElementsConfigured !== 'undefined' && stripeElementsConfigured) { %>
                      <p class="account-meta">Subscribe by selecting your plan preference and completing our simple checkout (powered by Stripe)</p>
                    <% } else { %>
                      <p class="account-meta">Choose billing interval — secure checkout via Stripe. Add <strong>STRIPE_PUBLISHABLE_KEY</strong> to pay on this site instead of redirecting to Stripe.</p>
                    <% } %>
                  <% } else { %>
                    <% if (typeof stripeElementsConfigured !== 'undefined' && stripeElementsConfigured) { %>
                      <p style="margin:.75rem 0 0"><a class="app-btn-primary" href="/billing/subscribe">Upgrade to member</a></p>
                      <p class="account-meta">Pay on the next page — card details stay with Stripe.</p>
                    <% } else { %>
                      <p style="margin:.75rem 0 0"><a class="app-btn-primary" href="/billing/checkout">Upgrade to member</a></p>
                      <p class="account-meta">Secure checkout via Stripe. Add <strong>STRIPE_PUBLISHABLE_KEY</strong> to pay on this site instead.</p>
                    <% } %>
                  <% } %>
                <% } else { %>
                  <p class="account-meta">Online billing isn’t active on this deploy yet — set <strong>STRIPE_SECRET_KEY</strong>, <strong>PUBLIC_BASE_URL</strong>, and either <strong>STRIPE_PRICE_MONTHLY</strong> + <strong>STRIPE_PRICE_YEARLY</strong> or a single <strong>STRIPE_PRICE_ID</strong>, then redeploy.</p>
                  <% if (typeof billingEnvMissing !== 'undefined' && billingEnvMissing.length) { %>
                    <p class="account-meta account-meta--warn">Missing from process env: <%= billingEnvMissing.join(', ') %></p>
                  <% } %>
                <% } %>
              <% } %>
            </section>

            <% if (stripeConfigured && subscriptionRow && subscriptionRow.stripe_customer_id) { %>
            <hr class="account-section-rule" aria-hidden="true" />
            <section class="account-rail-section" aria-label="Payment history">
              <h2 class="account-h2">Payment history</h2>
              <p class="account-meta">Completed charges on your membership (via Stripe). Official receipts are sent to your email when a payment succeeds.</p>
              <% if (typeof billingHistory !== 'undefined' && billingHistory.length) { %>
                <div class="account-table-scroll" role="region" aria-label="Past charges table">
                  <table class="account-billing-history-table">
                    <thead>
                      <tr>
                        <th scope="col">Date</th>
                        <th scope="col">Description</th>
                        <th scope="col">Amount</th>
                        <th scope="col">Status</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% billingHistory.forEach(function (row) { %>
                        <tr>
                          <td><%= row.dateLabel %></td>
                          <td><%= row.description %></td>
                          <td><%= row.amountLabel %></td>
                          <td><%= row.statusLabel %></td>
                        </tr>
                      <% }); %>
                    </tbody>
                  </table>
                </div>
              <% } else { %>
                <p class="account-meta">No paid invoices on file yet. After your first successful payment, entries will appear here.</p>
              <% } %>
            </section>
            <% } %>
          </div>
        </div>
      </div>
    </main>
  </div>

  <div id="account-modal-cancel-renewal" class="account-modal-overlay" hidden>
    <div class="account-modal-overlay__backdrop" data-account-modal-dismiss="cancel-renewal" tabindex="-1" aria-hidden="true"></div>
    <div class="account-modal-overlay__panel" role="dialog" aria-modal="true" aria-labelledby="account-modal-cancel-renewal-title">
      <h3 id="account-modal-cancel-renewal-title" class="account-modal-overlay__title">Turning Off Auto-Renewal</h3>
      <p class="account-modal-overlay__text">Are you sure that you wish to turn off auto-renewal? Doing so will limit your access to the end of your billing period.</p>
      <div class="account-modal-overlay__actions">
        <button type="button" class="account-modal-overlay__btn account-modal-overlay__btn--keep" data-account-modal-dismiss="cancel-renewal">Keep Subscription</button>
        <button type="button" class="account-modal-overlay__btn account-modal-overlay__btn--confirm-cancel" id="account-modal-cancel-renewal-confirm">Confirmed, Cancel Subscription</button>
      </div>
    </div>
  </div>

  <div id="account-modal-yearly-plan" class="account-modal-overlay" hidden>
    <div class="account-modal-overlay__backdrop" data-account-modal-dismiss="yearly-plan" tabindex="-1" aria-hidden="true"></div>
    <div class="account-modal-overlay__panel" role="dialog" aria-modal="true" aria-labelledby="account-modal-yearly-title">
      <h3 id="account-modal-yearly-title" class="account-modal-overlay__title">Confirm the Change to Annual Plan</h3>
      <p class="account-modal-overlay__text">Stripe will prorate the rest of this billing period (charge or credit on your next invoice).</p>
      <p id="account-modal-yearly-estimate" class="account-modal-overlay__text account-modal-overlay__text--estimate"></p>
      <div class="account-modal-overlay__actions">
        <button type="button" class="account-modal-overlay__btn account-modal-overlay__btn--secondary" data-account-modal-dismiss="yearly-plan">Cancel</button>
        <button type="button" class="account-modal-overlay__btn account-modal-overlay__btn--confirm-mint" id="account-modal-yearly-confirm">Confirm</button>
      </div>
    </div>
  </div>

  <script src="/js/account.js" defer></script>
</body>
</html>
